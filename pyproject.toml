# https://docs.pytest.org/en/latest/explanation/goodpractices
# https://packaging.python.org/en/latest/guides/writing-pypro
[tool.poetry]
name = "git_tb"
version = "0.1.0"
description = "git toolbox"
authors = ["Alejandro Visiedo <alejandro.visiedo@gmail.com>"]
license = "APACHE-2"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pyyaml = ">=6.0.2,<6.1.0"

[tool.poetry.group.dev.dependencies]
pytest = ">=8.3.5,<8.4.0"
black = ">=25.1.0,<25.2.0"
pylint = ">=3.3.7,<3.4.0"
pdoc3 = ">=0.11.6,<0.12.0"
pytest-cov = ">=6.2.1,<6.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
